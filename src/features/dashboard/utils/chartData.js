const chartData = () => {
  // const res = inv.filter((i) =>
  //   data.find(
  //     (e) =>
  //       formatToUniversalDate(e.date) === formatToUniversalDate(i.item.saleDate)
  //   )
  // );
  // console.log(res);
};

export default chartData;
